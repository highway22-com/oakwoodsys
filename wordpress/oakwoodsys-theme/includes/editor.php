<?php
/**
 * Block editor integration.
 *
 * Editor styles are enqueued via includes/enqueue.php (enqueue_block_editor_assets).
 * assets/theme.json is kept as reference only — not merged at runtime (avoids WP version conflicts).
 *
 * @package OakwoodsysTheme
 */

defined( 'ABSPATH' ) || exit;
